# MCP External API v1 实施说明（暂停）

MCP 不在当前 CLI 交付周期范围内。本文件保留为历史设计记录，不属于本期 Skill、CLI 文档、默认测试门禁或发布说明；其中的工具、传输和启动方式当前均不作为可用能力对外承诺。

如后续重新纳入 MCP，需要单独确认协议、认证、传输安全、工具边界、测试门禁和发布流程，并同步更新 `AGENTS.md`、`README.md` 与 Skill。当前请只使用 `wp` CLI。
