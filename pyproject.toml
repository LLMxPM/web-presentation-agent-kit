[tool.uv.workspace]
members = ["packages/api-client", "packages/cli", "mcp-server"]

[tool.uv]
default-groups = ["dev"]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
]

[tool.pytest.ini_options]
pythonpath = ["packages/api-client/src", "packages/cli/src"]
